package jacz.peerengineclient.data;

/**
 * Actions that can be performed when adding a file to the api
 */
public enum MoveFileAction {

    MOVE_TO_MEDIA_REPO,
    MOVE_TO_IMAGE_REPO,
    DO_NOT_MOVE
}
